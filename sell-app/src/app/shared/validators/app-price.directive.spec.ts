import { AppPriceDirective } from './app-price.directive';

describe('AppPriceDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPriceDirective();
    expect(directive).toBeTruthy();
  });
});
