import { AppImageDirective } from './app-image.directive';

describe('AppImageDirective', () => {
  it('should create an instance', () => {
    const directive = new AppImageDirective();
    expect(directive).toBeTruthy();
  });
});
